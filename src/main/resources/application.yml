spring:
  
      jpa:
          hibernate:
            ddl-auto: validate
          properties:
            hibernate:
              hibernate.enable_lazy_load_no_trans: true
              show_sql: true
              use_sql_comments: true
              format_sql: true

      datasource:
           url: jdbc:mysql://localhost:3306/projetgarage1?zeroDateTimeBehavior=CONVERT_TO_NULL&serverTimezone=UTC
           username: root
           password: andreayew